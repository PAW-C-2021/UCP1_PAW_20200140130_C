@model IEnumerable<UCP_PAW_1.Models.Kela>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Data Kelas</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Nama Kelas
            </th>
            <th>
                Id Mapel
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NamaKelas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdMapelNavigation.IdMapel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdKelas"><i class="fa fa-marker fa-lg"></i></a> |
                <a asp-action="Details" asp-route-id="@item.IdKelas"><i class="fas fa-list"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.IdKelas"><i class="fa fa-trash-alt fa-lg"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
